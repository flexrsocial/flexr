# Python
__pycache__/
*.pyc
.pytest_cache/

# venv
backend/venv/

# Secrets — nur .env.example wird versioniert
backend/.env

# Claude Code lokale Einstellungen (maschinenspezifisch)
.claude/settings.local.json

# OS
.DS_Store

# Android/TWA-Build (Keystore + Build-Artefakte niemals versionieren)
android/android.keystore
android/*.keystore
android/*.apk
android/*.aab
android/*.idsig
android/KEYSTORE-CREDENTIALS.txt
android/build/
android/app/
android/gradle*
android/.gradle/
android/store_icon.png
# Play-Store-Marketing-Assets (generiert, nicht versioniert)
android/store/
# App-Store-Screenshots ebenso — der Generator und die Texte bleiben drin
ios/store/out/
android/.buildenv.sh
android/*.log
# Von Bubblewrap generierte Projektdateien (werden von build.sh neu erzeugt)
android/build.gradle
android/settings.gradle
android/manifest-checksum.txt

# Lokale Testumgebung
backend/.env.test
